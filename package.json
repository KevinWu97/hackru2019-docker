{
  "name": "nodesample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "raf": "rimraf package-lock.json",
    "preraf": "echo hello",
    "start": "node test.js",
    "deploy": "heroku create && heroku container:push web && heroku container:release web"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "rimraf": "^3.0.0"
  }
}
